-- Indent-Blankline is a Neovim plugin that adds indentation guides to all lines (including empty lines).
return {
	"lukas-reineke/indent-blankline.nvim",
	main = "ibl",
	opts = {
		scope = {
			enabled = false,
		},
	},
}
